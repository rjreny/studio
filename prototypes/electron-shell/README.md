# Studio — Electron bakeoff prototype

Independent implementation of the same shell spec. Uses `titleBarStyle: hidden` + `titleBarOverlay`.

```bash
cd prototypes/electron-shell
npm install
npm run dev
npm run build
```

Installer output: `release/`.
