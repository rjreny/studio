# Studio — Tauri bakeoff prototype

Stressful desktop shell for the runtime bakeoff. Not the real Studio app.

```bash
cd prototypes/tauri-shell
npm install
npm run tauri dev
npm run tauri build
```

NSIS installer lands in `src-tauri/target/release/bundle/nsis/`.
